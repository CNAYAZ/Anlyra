import { FileText } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function ReportsBuilderPage() {
  return <PagePlaceholder pageKey="reportsBuilder" icon={FileText} />;
}

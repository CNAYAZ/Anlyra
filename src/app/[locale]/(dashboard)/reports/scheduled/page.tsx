import { FileText } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function ReportsScheduledPage() {
  return <PagePlaceholder pageKey="reportsScheduled" icon={FileText} />;
}

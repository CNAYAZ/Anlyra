import { Brain } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AIBenchmarksPage() {
  return <PagePlaceholder pageKey="aiBenchmarks" icon={Brain} />;
}

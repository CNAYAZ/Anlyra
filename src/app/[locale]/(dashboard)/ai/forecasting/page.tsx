import { Brain } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AIForecastingPage() {
  return <PagePlaceholder pageKey="aiForecasting" icon={Brain} />;
}

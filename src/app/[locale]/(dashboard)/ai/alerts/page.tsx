import { Brain } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AIAlertsPage() {
  return <PagePlaceholder pageKey="aiAlerts" icon={Brain} />;
}

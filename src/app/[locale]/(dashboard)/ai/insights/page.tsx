import { Brain } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function AIInsightsPage() {
  return <PagePlaceholder pageKey="aiInsights" icon={Brain} />;
}

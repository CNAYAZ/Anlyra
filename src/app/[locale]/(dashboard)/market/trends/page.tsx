import { Globe } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function MarketTrendsPage() {
  return <PagePlaceholder pageKey="marketTrends" icon={Globe} />;
}

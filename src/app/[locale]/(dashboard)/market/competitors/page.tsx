import { Globe } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function MarketCompetitorsPage() {
  return <PagePlaceholder pageKey="marketCompetitors" icon={Globe} />;
}

import { Globe } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function MarketPositioningPage() {
  return <PagePlaceholder pageKey="marketPositioning" icon={Globe} />;
}

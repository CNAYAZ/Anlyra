import { ShieldCheck } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function SettingsSecurityPage() {
  return <PagePlaceholder pageKey="settingsSecurity" icon={ShieldCheck} />;
}

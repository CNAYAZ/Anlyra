import { Settings as SettingsIcon } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function SettingsPage() {
  return <PagePlaceholder pageKey="settings" icon={SettingsIcon} />;
}

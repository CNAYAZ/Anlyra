import { Users } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function SettingsTeamPage() {
  return <PagePlaceholder pageKey="settingsTeam" icon={Users} />;
}

import { Activity } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function OperationsTeamPage() {
  return <PagePlaceholder pageKey="operationsTeam" icon={Activity} />;
}

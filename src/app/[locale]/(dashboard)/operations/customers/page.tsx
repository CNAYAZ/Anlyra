import { Activity } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function OperationsCustomersPage() {
  return <PagePlaceholder pageKey="operationsCustomers" icon={Activity} />;
}

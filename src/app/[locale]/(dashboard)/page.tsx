import { Home } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function OverviewPage() {
  return <PagePlaceholder pageKey="overview" icon={Home} />;
}

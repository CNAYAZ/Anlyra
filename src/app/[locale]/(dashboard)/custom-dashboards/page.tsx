import { Layout } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function CustomDashboardsPage() {
  return <PagePlaceholder pageKey="customDashboards" icon={Layout} />;
}

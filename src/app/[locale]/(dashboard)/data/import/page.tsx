import { Database } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function DataImportPage() {
  return <PagePlaceholder pageKey="dataImport" icon={Database} />;
}

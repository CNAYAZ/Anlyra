import { Database } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function DataHistoryPage() {
  return <PagePlaceholder pageKey="dataHistory" icon={Database} />;
}

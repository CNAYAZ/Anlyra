import { Database } from 'lucide-react';
import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function DataManualPage() {
  return <PagePlaceholder pageKey="dataManual" icon={Database} />;
}
